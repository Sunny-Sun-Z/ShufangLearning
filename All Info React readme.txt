1. get node.js installed
2. get typescript install

1. creat a project:  
	npx create-react-app my-app
	cd my-app
	npm start

create typescript app:
	npx create-react-app SunnyTypscript --template typescript

2. will open http://localhost:3000/

3.When you’re ready to deploy to production, create a minified bundle with npm run build.

4. basic: npx -p @storybook/cli sb init  
	Create React App doesn’t include any tools for this by default, but you can add Storybook for React (source)

5. Analyzing the Bundle Size

To add Source map explorer to a Create React App project, follow these steps:
	npm install --save source-map-explorer

	npm run build
	npm run analyze

------------------------------------------------------------------
1. npm create vite@latest
2. npm instal
3. npm dev
4. get url: http://localhost:5173/

